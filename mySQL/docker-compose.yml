version: '3.1'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    container_name: sistemaDiretor
    environment:
      MYSQL_ROOT_PASSWORD: letmein
      MYSQL_DATABASE: sistemaDiretor
    ports:
      - 3306:3306